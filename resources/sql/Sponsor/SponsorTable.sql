CREATE TABLE Sponsor(
	id SERIAL,
	name VARCHAR(80),
	email VARCHAR(80),
	web VARCHAR(80),
	facebook VARCHAR(80),
	instagram VARCHAR(80),
	youtube VARCHAR(80),
	twitter VARCHAR(80),
	PRIMARY KEY(id)
);
